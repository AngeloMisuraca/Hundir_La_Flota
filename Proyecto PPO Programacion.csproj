<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net9.0</TargetFramework>
    <RootNamespace>Proyecto_PPO_Programacion</RootNamespace>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Excluimos archivos temporales de compilaciones anteriores para evitar clases y atributos duplicados. -->
    <Compile Remove="temp_build\**\*.cs" />
  </ItemGroup>

</Project>
